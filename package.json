{
	"private": true,
	"type": "module",
	"scripts": {
		"postinstall": "cd desk && yarn install",
		"dev": "cd desk && yarn dev",
		"build": "cd desk && yarn build"
	},
	"dependencies": {
		"vite-plugin-pwa": "^1.0.2"
	}
}